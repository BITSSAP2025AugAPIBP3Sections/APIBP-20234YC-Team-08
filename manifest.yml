---
applications:
  - name: mnist-streamlit-app
    memory: 512M
    buildpacks:
      - python_buildpack
    command: streamlit run src/app.py --server.port $PORT --server.enableCORS false
    env:
      STREAMLIT_SERVER_PORT: 8080

