import com.pswidersk.gradle.python.VenvTask;

plugins {
  id("com.pswidersk.python-plugin") version "2.10.0"
}

pythonPlugin {
    pythonVersion = "3.11"
}

// tasks.register("cloneRepo", Exec) {
//     commandLine 'git', 'clone', 'git@github.com:BITSSAP2025AugAPIBP3Sections/APIBP-20234YC-Team-08.git'
// }

tasks.register("setupVenv", VenvTask) {
    venvExec = "python"
    args = ["-m", "pip", "install", "-r", "requirements.txt"]
}

tasks.register("runDemo", VenvTask) {
    venvExec = "python"
    args = ['-m', 'streamlit', 'run', 'app.py']
}